# GitHub Release Integration Notes

**Repository:** `https://github.com/yokken0907/hubble-tension-full-reproducibility`  
**Repository release:** `v1.1`

This tree is designed to be the complete source state for repository release v1.1. The repository has not yet been registered or released; v1.1 is the intended public release tag for this source state.

Release integration rules:

1. Create the GitHub repository from this exact tree and publish tag/release `v1.1`.
2. Publish the v1.1 tree without manuscript or supplement PDFs; the repository distribution is complete without those files.
3. Use the repository-level `MANIFEST.tsv` and `SHA256SUMS.txt` generated from this exact tree.
4. Keep release metadata factual and version-specific; repository metadata do not enlarge the scientific claims.
5. Do not modify the version-locked historical records merely to normalize old terminology or machine-specific paths; current public-facing documents provide the normalized interpretation.

The repository URL is recorded in `CITATION.cff`. The software license is the standard MIT License; third-party source-product notices are kept separately in `THIRD_PARTY_NOTICES.md`.

# AI assistance disclosure

This audit package was designed, implemented, tested, documented, and
packaged with substantial assistance from OpenAI ChatGPT Work (Codex).
Keiji Yoshimura directed the research scope, supplied the governing
scientific constraints, and is responsible for the decision to retain and
release the package.

All machine-readable results are reproducible from the frozen public inputs
and the included scripts. AI-generated reasoning is not treated as evidence;
only source-locked public bytes and deterministic computations enter the
formal classifications.

The script named `independent_verify.py` is a second implementation produced
and run within the same AI-assisted project. Its agreement is an internal
cross-check, not an external independent replication, peer review, expert approval, or endorsement.

# HTS60 selection audit

Selected endpoints and release edges are exactly those frozen in HTS59. No new root, model,
likelihood, parameter or approximate edge is introduced. The original-to-fixed contrast remains
a contract-mismatched release endpoint comparison, not a bugfix-only contrast.

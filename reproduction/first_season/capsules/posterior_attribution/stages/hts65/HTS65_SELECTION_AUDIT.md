# HTS65 selection audit

HTS65 uses exactly the existing four conditional coordinates and exhausts their finite set of
15 set partitions. No partition is chosen after seeing results. No new model, likelihood,
posterior root, chain, parameter or approximate endpoint edge is introduced.
